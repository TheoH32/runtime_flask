version: '3'
services:
        web:
                image: runtime_terror_v1
                build: .
                ports:
                        - "8032:8032"
                volumes:
                        - ./volumes:/volumes
                        - ./instance:/instance
                restart: unless-stopped
